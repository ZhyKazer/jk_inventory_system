rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Development-only: allow local emulator read/write.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
